services:
  mt5linux:
    image: lprett/mt5linux:latest
    container_name: mt5linux
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - LOGIN=${MT5_LOGIN:-}
      - PASSWORD=${MT5_PASSWORD:-}
      - SERVER=${MT5_SERVER:-}
